#!/bin/bash

# Display, whether a task is done or not.
set -eu

OUTPUT=$(taskoutput "$@" 2> /dev/null)

if [ $? -ne 0 ]; then
    echo "Usage: taskstatus TASKNAME"
    exit 1
fi

if [ ! -e "$OUTPUT" ]; then
    echo "TODO $OUTPUT"
    exit 1
else
    echo "DONE $OUTPUT"
    exit 0
fi
